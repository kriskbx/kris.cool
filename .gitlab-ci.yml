stages:
  - init
  - build
  - deploy

disable_cache: true

install-dependencies:
  stage: init
  script:
  - npm-install-missing
  - bundler install

build:
  stage: build
  script:
  - cd src && jekyll build
  - cd ../ && grunt --force
  artifacts:
    paths:
    - src/_site/

deploy-to-live:
  stage: deploy
  only:
  - release
  script:
  - grunt deploy

deploy-to-test:
  stage: deploy
  only:
  - master
  script:
  - grunt deploy:test